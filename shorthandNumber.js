var price = 20;
price += 50; // this will work only when you take the number in same variable

console.log(price);

var price = 60;
price -= 50; // this will work only when you take the number in same variable

console.log(price);
